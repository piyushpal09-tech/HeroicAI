const CursorLogo = ({ className = '' }) => (
  <span className={`font-display text-lg font-semibold tracking-[0.18em] text-foreground ${className}`}>
    PiyushAI
    <span className="ml-1 inline-block h-5 w-[2px] animate-cursor bg-primary align-middle" />
  </span>
)

export default CursorLogo
